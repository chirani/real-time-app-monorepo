{
  "name": "rocketchat_clone",
  "version": "1.0.0",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently \"bun run --filter web dev\" \"bun run --filter server dev\""
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
